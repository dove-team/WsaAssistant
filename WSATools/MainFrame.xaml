<hc:BlurWindow x:Class="WSATools.MainFrame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WSATools" mc:Ignorable="d"
        ResizeMode="NoResize" xmlns:vm="clr-namespace:WSATools.ViewModels"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        Title="MainFrame" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainFrameViewModel x:Name="viewModel" />
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:CallMethodAction MethodName="LoadAsync" TargetObject="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="54" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <Button>主页</Button>
        </StackPanel>
        <Frame Grid.Column="1">
            
        </Frame>
    </Grid>
</hc:BlurWindow>
